# Harness core maintenance binary
scripts/bin/harness
scripts/bin/harness.exe

# Isolated implementation worktrees
.worktrees/
node_modules/
.next/
coverage/
playwright-report/
test-results/
.env
.env.*
.env.e2e
!.env.example
!.env.e2e.example
!.env.production.example

# Local Google OAuth credential export
client_secret_google.json
.brainstorm/
~$*.xlsx

# Real attendance workbooks. They carry colleagues' names and hours, and the
# reference suite reads one by path rather than from the repository — see the
# `REFERENCE_XLSX_PATH` section of README.md.
*.xlsx
